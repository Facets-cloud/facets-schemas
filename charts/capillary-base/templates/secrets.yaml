apiVersion: v1
kind: Secret
metadata:
  name: "{{ template "capillary-base.fullname" . }}-configs"
type: Opaque
data:
  {{- range $index, $value := .Values.configurations }}
    {{ $index }}: {{ $value | b64enc }}
  {{- end }}