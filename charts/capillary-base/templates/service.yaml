apiVersion: v1
kind: Service
metadata:
  name: {{ template "capillary-base.fullname" . }}
  labels:
    app: {{ template "capillary-base.name" . }}
    chart: {{ template "capillary-base.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "capillary-base.name" . }}
    release: {{ .Release.Name }}
