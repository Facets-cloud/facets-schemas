apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name  }}-credentials"
type: Opaque
data:
  {{- range $index, $value := .Values.credentials }}
    {{ $index }}: {{ $value | b64enc }}
  {{- end }}